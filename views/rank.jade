doctype html
head
    meta(charset="utf-8")
    meta(http-equiv="X-UA-Compatible", content="IE=edge")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    title Vote: #{title}
    link(rel="stylesheet",
        href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css")
    link(rel="stylesheet", type="text/css",
        href="//code.jquery.com/ui/1.11.4/themes/smoothness/jquery-ui.css")
    link(rel="stylesheet", type="text/css", href="css/rozinah.css")
    include mixpanel.jade
body
    include header.jade
    div.container-fluid
      div.form-group.col-sm-7
        h2
          = title
          p(class="text-muted small")
            = description
        include rank_sharebox.jade
      form(role="form", id="votedeckrank", method="post", action="/vote/" + rankid)
        div.form-group.col-sm-7
            label(for="voter") Your name
            input(type="text", class="form-control", id="voter",
                name="voter", placeholder="Enter your name",
                required="required", maxlength="40")
            - var voter_mini_label = email ? "So others know what you voted for":"So the creator of the poll knows who voted";
            p(class="text-info small") #{voter_mini_label}
        div.form-group.col-sm-7
          label(for="sortable") Grab, drag, reorder and then submit
          ul(id="sortable")
              each option, index in options
                  li.ui-state-default
                      = option
                      input(id="#{option}", name="option#{index}", value="#{option}", type="hidden")
        if !email
            div.form-group.col-sm-7
              label(for="email") Your email
              input(type="email", id="email", name="email", required="required",
                    class="form-control", autocomplete="on", maxlength="40")
              p(class="text-info small")
                | We'll send you an email with a link to the results. No spam
        div.form-group.col-sm-7
            button(type="submit", value="submit", class="btn btn-default")
                | Submit Your Vote
    include footer.jade
    script(src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js")
    script(src="//code.jquery.com/ui/1.11.4/jquery-ui.js")
    script(src="js/jquery.ui.touch-punch.min.js")
    script(src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js")
    script(src="js/rozinah.js")
